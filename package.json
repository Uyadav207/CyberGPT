{
	"scripts": {
		"check:fix:frontend": "cd frontend && biome check --write ./src && tsc -b && vite build",
		"check:fix:backend": "cd backend && bun check",
		"prepare": "husky"
	},
	"devDependencies": {
		"@types/react-syntax-highlighter": "^15.5.13"
	},
	"dependencies": {
		"@types/d3": "^7.4.3",
		"aframe": "^1.7.1",
		"d3": "^7.9.0",
		"stripe": "^17.6.0"
	}
}
